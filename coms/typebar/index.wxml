<!--coms/wishbar/index.wxml-->
<view class="typebar-container">
  <view class="list" wx:for="{{typeMap}}" wx:key="{{item}}">
    <button class="check-btn"  mark:type="{{index}}" bindtap="typeChange">{{item}}</button>
  </view>
</view>
